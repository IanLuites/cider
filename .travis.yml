sudo: false
language: elixir
elixir:
  - 1.8.1
otp_release:
  - 21.3
install:
  - mix local.rebar --force
  - mix local.hex --force
  - mix deps.get
after_script:
  - MIX_ENV=test mix do deps.get, compile, coveralls.travis
cache:
  directories:
    - _build
    - deps
